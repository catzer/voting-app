apiVersion: v1
kind: Service
metadata:
  name: redis-sts-service
  labels:
    app: demo-voting-app
    name: redis-sts-service
spec:
  clusterIP: None
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: demo-voting-app
    name: redis-pod